name: Build Vue App
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v4
    - id: Build-Vue
      uses: xRealNeon/VuePagesAction@1.0.1
      with:
        username: 'hotenov'
        reponame: 'bnipi-test-task-front'
        token: ${{ secrets.GITHUB_TOKEN }} # Leave this line unchanged
